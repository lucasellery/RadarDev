import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //criado pelo próprio projeto

ReactDOM.render(<App />, document.getElementById('root'));
//DOM está mandando renderizar(desenhando) dentro da div 'root'

//JSX (Javascript + HTML)